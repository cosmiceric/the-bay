{#
 # Topics index template
 #}

{% extends "_layout" %}
{% set title = "Topics" %}

{% block content %}
	<h1>Topics</h1>

	{% for entry in craft.categories('topics').find() %}
		<article>
			<h3><a href="{{ entry.url }}">{{ entry.title }}</a></h3>
		</article>
	{% endfor %}
{% endblock %}